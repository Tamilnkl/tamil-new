<p>
	<strong>Title:</strong>
	<%= @product.title %>
</p>

<p>
	<strong>Text:</strong>
	<%= @product.text_area %>
</p>
<p>
	<strong>Price:</strong>
	<%= @product.text %>
</p>

<%= link_to 'New login', new_product_path%>
<td> <%= link_to 'Show', @product %></td>
<td><%= link_to 'Edit', edit_product_path(@product) %></td>
<td><%= link_to 'Destroy', @product, confirm: 'Are you sure?', method: :delete %></td>