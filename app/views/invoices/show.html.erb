<h1>Listing Invoice</h1>
<table>
  <tr>
    <th>Name</th>
    <th>Phone</th>
  </tr>
  
    <tr>
      <td><%= @invoice.name %></td>
       <td><%= @invoice.phone %></td>
      <td><%= link_to 'Show', @invoice %></td>
      <td><%= link_to 'Edit', edit_invoice_path(@invoice) %></td>
      <td><%= link_to 'Destroy', @invoice, confirm: 'Are you sure?', method: :delete %></td>
    </tr>
  
</table>
<%= link_to 'New login', new_invoice_path%>

<style>
td, th {
    padding: 5px;
}
</style>
